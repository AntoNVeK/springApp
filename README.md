Социальное приложение с блогами и подписками, построенное на Spring Boot. Приложение предоставляет REST API для управления пользователями, постами, комментариями, профилями и системой подписок.


---

##  Основные сущности

1. **User (Пользователь)**  
   `id, userName, email, age, createdAt`  
   Основная сущность системы

2. **UserProfile (Профиль пользователя)**  
   `id, userId, fullName, bio, city, street, zipCode`  
   Расширенная информация о пользователе

3. **Post (Пост/Статья)**  
   `id, title, content, authorId, createdAt`  
   Публикации пользователей

4. **Comment (Комментарий)**  
   `id, text, postId, userId, parentCommentId, createdAt`  
   Иерархические комментарии (поддержка ответов)

5. **UserFollower (Подписка)**  
   `userId, followerId, createdAt`  
   Система подписок "кто на кого подписан"

---

##  API Endpoints

### Пользователи `/api/users`
| Метод | URL | Описание |
|-------|-----|----------|
| GET   | `/` | Получить всех пользователей |
| GET   | `/{id}` | Получить пользователя по ID |
| POST  | `/` | Создать нового пользователя |
| PUT   | `/{id}` | Обновить пользователя |
| DELETE| `/{id}` | Удалить пользователя |
| GET   | `/search` | Поиск по имени или email |
| GET   | `/count` | Количество пользователей |

### Профили `/api/users/{userId}/profile`
| Метод | URL | Описание |
|-------|-----|----------|
| GET   | `/` | Получить профиль пользователя |
| POST/PUT | `/` | Создать/обновить профиль |

### Посты `/api/posts`
| Метод | URL | Описание |
|-------|-----|----------|
| GET   | `/` | Все посты |
| GET   | `/{id}` | Пост по ID |
| POST  | `/` | Создать пост |
| GET   | `/with-comment-count` | Посты с количеством комментариев |

### Комментарии (через сервисный слой)
| Метод | URL | Описание |
|-------|-----|----------|
| GET   |  | Комментарии к посту |
| CRUD  |  | Стандартные операции |

### Подписки `/api/users/`
| Метод | URL | Описание |
|-------|-----|----------|
| POST   | `/{userId}/follow/{followerId}` | Подписаться |
| DELETE | `/{userId}/unfollow/{followerId}` | Отписаться |
| GET    | `/followers` | Получить подписчиков |
| GET    | `/{followerId}following` | Получить подписки |


---

## 🛠 Технологический стек

- **Spring Boot 3.x** — основной фреймворк  
- **Spring Web MVC** — REST API  
- **Spring JDBC (JdbcTemplate)** — доступ к БД  
- **PostgreSQL** — база данных  
- **Lombok** — генерация boilerplate кода  

